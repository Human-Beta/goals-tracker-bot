# Post-MVP Roadmap (Bot)

Potential extensions after MVP stabilization:

- Webhook mode with signature verification (instead of polling-only runtime).
- Rich multi-step conversational flows with state machine/session layer.
- Localization and per-user language preferences.
- Notification/reminder scheduler aligned with user timezone.
- Command analytics and funnel tracking.
- Circuit-breaker strategy for downstream API instability.
